int main()
{
  float n, p;
  printf("entrer les deux nombres");
  scanf("%f%f", &n,&p);
  if(n>p)
   printf("l'ordre croissant des valeurs est:%f, %f", p, n);
 else
  printf("l'ordre croissant des valeurs est:%f, %f", n, p);
 }
